feat: performance enhancements

- Solved Suboptimal dictionary populating in nested loops
- Removed Unused import 'get_trend_data'
- Replaced the arrow function in the `$derived` store for `sortedHeadlines` with a `$derived.by` block.
- Refactored `loadRegionDetails` to use `Promise.allSettled` to fetch trend data and AI insights concurrently instead of sequentially awaiting them.
