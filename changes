refactor: isolate sentiment inference in short-lived subprocess

- move scraping and ONNX inference into collection_runner.py
- keep Celery worker focused on orchestration only
- run collection in a short-lived subprocess so model memory is released on exit
- make analyzer imports truly lazy so transformers/optimum are not loaded at worker idle
- keep worker memory settings aligned for low-RAM operation
- improve subprocess result parsing and error handling for collection tasks
